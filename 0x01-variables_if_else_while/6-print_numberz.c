#include <stdio.h>
/**
 * main - prints number between 0 to 9
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;
	for (i = 0 ; i < 50; i++);
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
